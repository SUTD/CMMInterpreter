/*	测试文件说明：
*	主题：数组与负数的相关测试
*	内容：测试real类型和int类型的数组；
*		 测试数组元素赋值时的强制类型转换；
*		 数组下标越界检测；
*/
real array[3];	//	声明real数组
array[0] = 1;	//	给浮点类型赋以整型值，将把整型值转为real再赋值
array[1] = 0.9876;		//	一般的赋值
array[2] = -3.5;		//	负数赋值
array[3] = 2.2;			//	下标越界检测：下标值大于数组长度
int index[1];	//	声明int数组
index[-1] = 0;	//	下标越界检测：下标值小于0的检测
index[0] = 0.7;	//	为整型数组元素赋以浮点类型的值，将把浮点型的值转为整型再赋值
while(index[0] < 3)
{	write(array[index[0]]);
	index[0] = index[0] + 1;
}

/*output:
//两个错误提示
1.0000
0.9876
-3.5000
*///:~
